export interface IPanelAddUserState {
  /**
   * put your state attributes here, for example:
   * myAttribute: any;
   */
}

export const PanelAddUserDefaultState = (): IPanelAddUserState => {
  return {
    /**
     * put your default value here, for example:
     * myAttribute: null,
     */
  };
};
