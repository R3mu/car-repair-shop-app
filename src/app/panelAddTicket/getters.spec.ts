import { PanelAddTicketGetters } from './getters';
import { PanelAddTicketDefaultState, IPanelAddTicketState } from './state';

describe('PanelAddTicketGetters', () => {
  let testState: IPanelAddTicketState;

  beforeEach(() => {
    testState = PanelAddTicketDefaultState();
  });

  test('Please write the tests for the getters here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/getters.spec.ts
    expect(true).toBeFalsy();
  });
});
