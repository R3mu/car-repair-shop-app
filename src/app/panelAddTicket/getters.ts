import { IPanelAddTicketState } from './state';

export interface IPanelAddTicketGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<IPanelAddTicketState, IState>, param: any): Promise<any>;
   */
}

export const PanelAddTicketGetters: IPanelAddTicketGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: IPanelAddTicketState) {
   *   return state.myAttribute;
   * }
   */
};
