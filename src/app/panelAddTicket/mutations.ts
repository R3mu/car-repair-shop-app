import { IPanelAddTicketState } from './state';

export interface IPanelAddTicketMutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<IPanelAddTicketState, IState>, param: any): void;
   */
}

export const PanelAddTicketMutations: IPanelAddTicketMutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
