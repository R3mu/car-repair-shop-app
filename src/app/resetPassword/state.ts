export interface IResetPasswordState {
  /**
   * put your state attributes here, for example:
   * myAttribute: any;
   */
}

export const ResetPasswordDefaultState = (): IResetPasswordState => {
  return {
    /**
     * put your default value here, for example:
     * myAttribute: null,
     */
  };
};
