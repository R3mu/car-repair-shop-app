import { ActionContext } from 'vuex';
import { IPanelEditTicketState } from './state';
import { IState } from '@/app/state';
import { HttpService } from '@/app/shared/services/HttpService/HttpService';

export interface IPanelEditTicketActions {
  /**
   * put your action names, parameters and return values here, for example:
   * myAction(context: ActionContext<IPanelEditTicketState, IState>, param: any): Promise<any>;
   */
}

export const PanelEditTicketActions: IPanelEditTicketActions = {
  /**
   * here comes the implementation of your actions, for example:
   * myAction({ commit }, param) {
   *   commit('MY_MUTATION);
   * }
   */
};
