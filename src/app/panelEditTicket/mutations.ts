import { IPanelEditTicketState } from './state';

export interface IPanelEditTicketMutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<IPanelEditTicketState, IState>, param: any): void;
   */
}

export const PanelEditTicketMutations: IPanelEditTicketMutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
