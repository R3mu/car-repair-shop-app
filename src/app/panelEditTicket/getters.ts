import { IPanelEditTicketState } from './state';

export interface IPanelEditTicketGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<IPanelEditTicketState, IState>, param: any): Promise<any>;
   */
}

export const PanelEditTicketGetters: IPanelEditTicketGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: IPanelEditTicketState) {
   *   return state.myAttribute;
   * }
   */
};
