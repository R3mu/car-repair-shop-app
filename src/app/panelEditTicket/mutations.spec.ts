import { PanelEditTicketMutations } from './mutations';
import { PanelEditTicketDefaultState, IPanelEditTicketState } from './state';

describe('PanelEditTicketMutations', () => {
  let testState: IPanelEditTicketState;

  beforeEach(() => {
    testState = PanelEditTicketDefaultState();
  });

  test('Please write the tests for the mutations here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/mutations.spec.ts
    expect(true).toBeFalsy();
  });
});
