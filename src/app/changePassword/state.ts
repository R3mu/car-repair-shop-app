export interface IChangePasswordState {
  /**
   * put your state attributes here, for example:
   * myAttribute: any;
   */
}

export const ChangePasswordDefaultState = (): IChangePasswordState => {
  return {
    /**
     * put your default value here, for example:
     * myAttribute: null,
     */
  };
};
